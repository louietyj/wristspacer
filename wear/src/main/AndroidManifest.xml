<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="Wristspacer"
        android:icon="@android:drawable/ic_dialog_info"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Declare this as a standalone Wear OS app (not a phone companion sideload).
             Required on Wear OS 3+ for the WearableListenerService to start correctly. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!--
            WearableListenerService: receives /next_event DataItems pushed from the phone.
            Path filter removed from manifest to avoid Wear OS 4 GMS delivery failures;
            we filter by path in onDataChanged() instead.
        -->
        <service
            android:name=".PhoneDataListenerService"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*" />
            </intent-filter>
        </service>

        <!--
            ComplicationDataSourceService: provides the next-event complication to watch faces.

            BIND_COMPLICATION_PROVIDER: only the Wear OS system may bind to this service.
            SUPPORTED_TYPES: SHORT_TEXT for small slots, LONG_TEXT for wide slots.
            UPDATE_PERIOD_SECONDS=0: updates are push-driven via requestUpdateAll()
                                     from PhoneDataListenerService; no polling needed.
        -->
        <service
            android:name=".NextEventComplicationService"
            android:label="Next Calendar Event"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,LONG_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="300" />
        </service>

    </application>

</manifest>
